# Authentik 配置文件
# 复制此文件为 .env.authentik 并修改相关配置

# PostgreSQL 配置
PG_PASS=authentik-secure-password-2024
PG_USER=authentik
PG_DB=authentik

# Authentik 密钥（请修改为随机字符串，至少50个字符）
# 生成命令: openssl rand -base64 60
AUTHENTIK_SECRET_KEY=please-change-this-to-a-very-long-random-secret-key-for-production

# Authentik 配置
AUTHENTIK_ERROR_REPORTING__ENABLED=false
AUTHENTIK_LOG_LEVEL=info

# 可选：SMTP 邮件配置（用于发送验证邮件、密码重置等）
# AUTHENTIK_EMAIL__HOST=smtp.gmail.com
# AUTHENTIK_EMAIL__PORT=587
# AUTHENTIK_EMAIL__USERNAME=your-email@gmail.com
# AUTHENTIK_EMAIL__PASSWORD=your-app-password
# AUTHENTIK_EMAIL__USE_TLS=true
# AUTHENTIK_EMAIL__FROM=noreply@microvibe.com
